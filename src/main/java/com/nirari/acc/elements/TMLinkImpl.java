package com.nirari.acc.elements;

import org.openqa.selenium.WebElement;

public class TMLinkImpl extends TMElementImpl implements TMLink {

    public TMLinkImpl(WebElement element) {
        super(element);
    }
}
